version: '3.8'

services:
  instalens:
    build: .
    container_name: instalens
    ports:
      - "5000:5000"
    volumes:
      # Mount current directory to persist playwright session files on the host
      - .:/app
      # Exclude the host's virtual environment from overwriting container dependencies
      - /app/venv
    restart: unless-stopped
